[project]
name = "langchain-falkordb-demo"
version = "0.1.0"
description = "Runnable demo for langchain-falkordb"
requires-python = ">=3.10,<4.0"
dependencies = [
  "langchain-falkordb",
]

[tool.uv.sources]
langchain-falkordb = { path = "..", editable = true }
